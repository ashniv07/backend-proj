version: 0.0
Resources:
  - TargetService:
      Type: AWS::Lambda::Function
      Properties:
        Name: "app-backend-a"
        Alias: "live"
        CurrentVersion: "6"  # Current alias version
        TargetVersion: "5"   # The new version you're deploying to
